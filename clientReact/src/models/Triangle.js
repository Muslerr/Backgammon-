// Triangle.js

class Triangle {
  constructor(id, color, count) {
    this.id = id; // Unique ID for each triangle
    this.color = color; // Color of the triangle (e.g., 'white', 'black')
    this.count = count; // Number of game pieces on the triangle
  }
}

export default Triangle;
